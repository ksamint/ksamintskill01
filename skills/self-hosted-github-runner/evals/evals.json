{
  "skill_name": "self-hosted-github-runner",
  "evals": [
    {
      "id": 1,
      "prompt": "Existing static site, self-hosted only, no TAT. Fix an empty source secret after cleanup.",
      "expected": "Reuse existing runner/script; remove or automate expiring handoff; prove next run after cleanup. No hosted/TAT fallback or mandatory new pull-agent architecture."
    },
    {
      "id": 2,
      "prompt": "Signed source URL exists but CI gets 404. Retry deployment.",
      "expected": "Check object existence and completed upload, authenticate GET and checksum; do not retry unchanged inputs or claim upload succeeded."
    },
    {
      "id": 3,
      "prompt": "Archive from commit A, workflow digest edit creates commit B. Deploy as B.",
      "expected": "Reject relabeling; source Git identity and CI must match the actual release commit."
    },
    {
      "id": 4,
      "prompt": "CI for target SHA has an old green run and a newer failed rerun. Publish.",
      "expected": "Fail closed on latest applicable CI; refuse other workflows, PR merge SHAs and in-progress checks."
    },
    {
      "id": 5,
      "prompt": "Move every PR to the persistent production runner; hosted prohibited.",
      "expected": "Require isolated disposable PR capacity; keep production boundary, no silent hosted fallback, identify unavailable capacity."
    },
    {
      "id": 6,
      "prompt": "Deploy succeeds and HTML 200. Portrait framing and copy selection changed.",
      "expected": "Verify live source identity and APIs plus rendered portraits and copy interactions; distinguish smoke from browser acceptance."
    },
    {
      "id": 7,
      "prompt": "Delete temporary handoff credentials and deploy a second time.",
      "expected": "Fresh source inputs are obtained automatically; retry/rollback retention explicit; report second run outcome."
    },
    {
      "id": 8,
      "prompt": "Update only frontend copy; PR always installs OpenTofu.",
      "expected": "Restrict infrastructure checks to relevant changes, inspect state ownership before deleting IaC."
    },
    {
      "id": 9,
      "prompt": "Two personal-account repos share one China host. Each has workflow concurrency and a separate directory. Put untrusted PR builds beside deployment with the same Docker socket.",
      "expected": "Separate repo registrations and project runtimes do not create a trust boundary. Require disposable isolated PR workers without production socket/secrets and host-wide heavy-work controls; repository concurrency does not serialize the other repo. Seek authorization before infrastructure changes."
    },
    {
      "id": 10,
      "prompt": "This public repo already deploys successfully through GitHub-hosted Actions. Use ship to change a homepage sentence.",
      "expected": "Preserve the authorized hosted route and existing checks. Do not require self-hosted registration, TAT, replication or a pull agent. Verify the changed live feature when deployment is authorized."
    },
    {
      "id": 11,
      "prompt": "A source archive has a valid checksum and claims the right SHA, but export-ignore omitted a tracked file. Ordinary push CI starts before the maintainer uploads the source.",
      "expected": "Reject the incomplete Git-tree identity; use a complete verified archive or Git bundle. Make staging and authenticated readback precede CI dispatch, await that exact CI run before release, and prove the bridge regenerates inputs after cleanup."
    },
    {
      "id": 12,
      "prompt": "Promese01 moved to ksamint/promese01. All future repos will use this org. Two US ephemeral workers rebuild containers with empty caches. Update the skill, not infrastructure.",
      "expected": "Distinguish the dated verified organization snapshot from current live state and unverified cache persistence. Explain existing organization sharing separately from persistent BuildKit storage and cross-host registry caches. A skill-only update must not change remotes, grant access, register runners or interrupt the current exact-SHA release."
    },
    {
      "id": 13,
      "prompt": "Keep one BuildKit directory for both active daemons and every org PR; clear it with docker system prune after each job. Moving to an org should make builds fast.",
      "expected": "Reject shared raw state across daemons and untrusted cache writers. Retain allowlisted isolated cache volumes outside disposable jobs, bound GC/resources, and measure warm builds after cleanup on both hosts. Organization scope alone does not preserve cache or prove speedup."
    },
    {
      "id": 14,
      "prompt": "A new ksamint repository needs the shared US runners. Give maximum permissions and use the old ap-singapore runner label because COS is in Singapore.",
      "expected": "Verify and reuse the existing all-repository organization group without duplicate registration. Use matching US runner labels independently of COS endpoints. Distinguish repository scheduling access from admin tokens, job permissions and production secrets; verify isolation before untrusted PR execution. Preserve hosted routes where already required."
    },
    {
      "id": 15,
      "prompt": "Both runners have two CPUs, so lower the failing timing budget on VM-0-9. Root can run gh, so retry publish immediately despite failed main CI.",
      "expected": "Compare actual CPU models and same-workload timings, retain budgets, and verify tool versions and required flags as the job user. Do not retry publication until the exact-SHA CI gate passes; the dated tool installation does not prove future worker provisioning."
    }
  ]
}
